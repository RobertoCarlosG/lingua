{
  "buildCommand": "yarn workspace lingua-frontend build",
  "outputDirectory": "frontend/dist",
  "installCommand": "yarn install",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
